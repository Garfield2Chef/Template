<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.hpy.cn.zhtemplate">

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
            android:name=".base.Myapplication"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".mvp.Home.HomeActivity"/>
        <activity
                android:name=".mvp.Splash.SplashActivity"
                android:theme="@style/SplashTheme">
        </activity>
        <activity android:name=".mvp.WelcomeGuide.WeilcomeGuideActivity"/>
        <activity
                android:name=".mvp.Login.LoginActivity"
                android:theme="@style/NostatusTheme">

        </activity>
        <activity android:name=".mvp.Details.CaseDetailsActivity">
        </activity>

        <activity android:name=".wheel.test.MytestActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>